data:
  num_samples: 5
  path_to_raw: ./data/raw/tracks.csv
  remote: mlops_remote
  repo: .
  sample_num: 2
  version: AIRFLOW2.0
defaults:
- _self_
- data_features
- model/model
- experiment
hydra:
  mode: MULTIRUN
  sweeper:
    params:
      +model: rf, lr
